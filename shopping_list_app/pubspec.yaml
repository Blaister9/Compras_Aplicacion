name: shopping_list_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.0                       # Dart 3.8 ok

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  go_router: ^15.1.2
  flutter_hooks: ^0.21.2
  uuid: ^4.5.1
  json_annotation: ^4.9.0

  # Freezed 2.4.x  →  source_gen 1.x
  freezed_annotation: ^2.4.1

  # Persistencia local
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.15

  # Generadores (todos en source_gen 1.x)
  freezed: ^2.4.6               #  ← versión previa
  hive_generator: ^2.0.1
  json_serializable: ^6.7.1




flutter:

  uses-material-design: true


